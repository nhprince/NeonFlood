# NeonFlood v2.0.0 — Python dependencies
#
# Core tool:  Python stdlib only — no pip packages required.
# Optional:   Install PySocks for SOCKS5 / HTTP proxy support.
#
# Install core only:
#   pip install -r requirements.txt
#
# Install with proxy support:
#   pip install -r requirements.txt
#   pip install PySocks

# Optional — required only if using --proxy or --tor
PySocks>=1.7.1
